<%= form_with(model: association_type, local: true) do |form| %>
  <% if association_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(association_type.errors.count, "error") %> prohibited this association_type from being saved:</h2>

      <ul>
      <% association_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :association_type_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
